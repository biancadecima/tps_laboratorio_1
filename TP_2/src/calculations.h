#ifndef CALCULATIONS_H_
#define CALCULATIONS_H_

/**
* \brief calculate the average between two numbers
* \param amount number
* \param total number on which the average will be calculated
* \return the average
*/
float calculateAverage(float amount, int total);

/**
* \brief sort the array of players by confederation name and by player name
* \param amount number
* \param total number on which the percentage will be calculated
* \return In case of success it returns 1, otherwise zero
*/
float calculatePercentage(int amount, int total);

#endif /* CALCULATIONS_H_ */
